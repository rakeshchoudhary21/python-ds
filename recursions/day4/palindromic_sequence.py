
#Q1: length of longest palindromic sequence

def length_of_ps(X,Y,i,j):
    # here Y is reversed X
    # we will need to find lcs of X and Y. that is the length of longest palindrome
    pass

# Q2: minimum deletions needed to make a string palindrome
def min_deletion(X,Y,i,j):
    # find the longest palindromic subseq.
    # len(X) - len(longest palindromic subseq) -> deletions needed
    # len(Y) - len(longest palindromic subseq)-> additions needed
    pass

